import 'package:flutter/material.dart';

class CoronavirusMapScreen extends StatelessWidget {
  const CoronavirusMapScreen({Key? key}) : super(key: key);
  static const routeName = '/coronavirus-map';
  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
